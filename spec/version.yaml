tests:
  - name: version
    args:
      - --version
    exit-status: 0
    stdout: |
      Smoke v2.4.0

  - name: version-numeric
    args:
      - --version-numeric
    exit-status: 0
    stdout: |
      2.4.0
