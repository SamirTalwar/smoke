tests:
  - name: version
    args:
      - --version
    exit-status: 0
    stdout: |
      Smoke v2.3.1

  - name: version-numeric
    args:
      - --version-numeric
    exit-status: 0
    stdout: |
      2.3.1
